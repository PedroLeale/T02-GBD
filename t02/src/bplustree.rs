use std::cmp::Ordering;
use crate::register;
const DEFAULT_ORDER: usize = 4;

//TO-DO
//Should have Insert, Get and Delete
//Tree should hold the file and the nodes are the registers
struct BPlusTree{
    
}